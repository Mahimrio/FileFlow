{
  "name": "fileflow-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@10.9.3",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.10.7"
  }
}
